/* Indicadores derivados exclusivamente de window.SentinelAlertas.dados. */
(function(){'use strict';const A=()=>window.SentinelAlertas;function dataIso(v){const [d,m,a]=v.split('/');return `${a}-${m}-${d}`}function filtrosPadrao(){try{return JSON.parse(sessionStorage.getItem('sentinel-filtros')||'["crítico","médio","baixo"]')}catch(_){return ['crítico','médio','baixo']}}function filtrar(op={}){const niveis=new Set(op.niveis||filtrosPadrao());return A().dados.filter(a=>niveis.has(a.gravidade)&&(!op.inicio||dataIso(a.data)>=op.inicio)&&(!op.fim||dataIso(a.data)<=op.fim))}function agrupar(itens,chave){return itens.reduce((r,x)=>{const k=chave(x);r[k]=(r[k]||0)+1;return r},{})}function regiao(a){const l=a.local.toLowerCase();if(l.includes('zona leste')||l.includes('penha')||l.includes('artur alvim')||l.includes('sapopemba')||l.includes('vila jacuí'))return 'Zona Leste';if(l.includes('zona sul')||l.includes('capão')||l.includes('ipiranga')||l.includes('jabaquara')||l.includes('moema')||l.includes('ibirapuera')||l.includes('guarapiranga'))return 'Zona Sul';if(l.includes('zona norte')||l.includes('vila maria')||l.includes('cachoeira'))return 'Zona Norte';if(l.includes('centro')||l.includes('brás')||l.includes('barra funda')||l.includes('cambuci')||l.includes('sé')||l.includes('paulista'))return 'Centro e Oeste';return a.local.split(',')[0]}function tipo(a){const t=a.descricao.toLowerCase();if(t.includes('homic')||t.includes('morte')||t.includes('feminic')||t.includes('atropelamento'))return 'Ocorrências com vítimas';if(t.includes('acidente')||t.includes('trânsito')||t.includes('queda de barreira'))return 'Mobilidade e vias';if(t.includes('chuva')||t.includes('alagamento')||t.includes('desab'))return 'Clima e infraestrutura';return 'Outras ocorrências'}function ordenado(obj){return Object.entries(obj).sort((a,b)=>b[1]-a[1])}window.SentinelPainel={filtrar,agrupar,regiao,tipo,ordenado,dataIso};})();
